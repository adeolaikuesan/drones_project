// From router file! 


// router.get('/data', (req, res) => {
//     dataController.datam(req, res, (data) => {
//       console.log(data);
//     });
//   });

// router.get('/data', dataController.getData);

// I need GET allData, GET pilotData and POST pilotData and DELETE pilotData

// const {
//   getData,
//   getPilot,
//   setPilot,
//   deletePilot,
// } = require('../controllers/droneController')


// router.route('/').get(protect, getGoals).post(protect, setGoal)
// router.route('/:id').delete(protect, deleteGoal).put(protect, updateGoal)


// if (Array.isArray(serialNumbers) && serialNumbers.length > 0) {
//     console.log("It is an array")
//     router.get('/data/pilots', dataController.getPilot(serialNumbers));
//   } else {
//     console.log('serialNumbers is not an array or is empty');
//   }
